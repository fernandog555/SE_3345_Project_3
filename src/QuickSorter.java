public class QuickSorter extends <Comparable <T>>
{
    private enum PivotStrategy
    {
        FIRST_ELEMENT,
        RANDOM_ELEMENT,
        MEDIAN_OF_TWO_ELEMENTS,
        MEDIAN_OF_THREE_ELEMENTS
    }

    // Use for enum: PivotStrategy myVar = PivotStrategy.FIRST_ELEMENT;

    static timeedQuickSort<E extends Comparable<E>>(list ArrayList<E>)
    {

    }
}
